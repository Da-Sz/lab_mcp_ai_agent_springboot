github:
  owner: ${GITHUB_OWNER:test-owner}
  repo: ${GITHUB_REPO:test-repo}

anthropic:
  api-key: ${ANTHROPIC_API_KEY:test-anthropic-key}
  model: claude-opus-4-20250514
  timeout-seconds: 60

openai:
  api-key: demo
  model: gpt-4o-mini
  timeout-seconds: 60

gemini:
  api-key: ${GEMINI_API_KEY:test-gemini-key}
  #model: gemini-2.5-flash
  model: gemini-2.5-flash-lite
  timeout-seconds: 60

mcp:
  base-url: ${MCP_BASE_URL:http://localhost:3333}
  path: ${MCP_PATH:/mcp}